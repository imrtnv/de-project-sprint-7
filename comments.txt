Артем привет! спасибо большое за обратную связь! все замечания исправил)

Пару комментариев про метод который я использую у себя в коде (это решение/подсказка наставника)
#расчет метики local_time - формула расчета описана в документации
df_local_time = (
        df_act_city
        .withColumn("timezone", F.concat(F.lit("Australia/"), F.col('act_city')) )
        .withColumn("local_time", F.from_utc_timestamp(F.col("date"),F.col('timezone')))
        .drop("timezone", "date", "act_city"))

и как ты указал в комментариях он может падать (когда не находит связь) так и есть!
я пытался использовать твой метод но и там он не все связки находит (и как мне сказали файл сам по себе не ок)
Если я оставляю два (основных города скрипт отрабатывает), больше - нет (не находит)

Спросил у одногрупников они прошли так,использовав метода наставника (с падающим airflow)

наверно я могу в файле geo.csv создать вспомогательный столбец именно для localtime subcity, прогнав координаты из файла с библиотекой tzwhere
но это такой же костыль какой и я соорудил ) оставив два города 
как мне сказали ошибка в самом файле с координатами которые не видит библиотека